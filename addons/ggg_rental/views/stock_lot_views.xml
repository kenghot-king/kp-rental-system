<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="stock_lot_view_form_inherit_rental" model="ir.ui.view">
        <field name="name">stock.lot.form.inherit.rental</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="stock.view_production_lot_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_damage_logs"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-exclamation-triangle"
                    invisible="damage_count == 0">
                    <field name="damage_count" widget="statinfo" string="Damages"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
